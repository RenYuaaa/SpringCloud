server:
  port: 9003

spring:
  application:
    name: NACOS-PAYMENT-PROVIDER
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'